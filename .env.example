# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017
DATABASE_NAME=email_client

# Session Configuration (Generate a random secret key)
SECRET_KEY=change-this-to-a-random-secret-key

# Note: All other settings (Resend API, Cloudflare R2) are configured
# through the web interface in Settings panel after initial setup
